# Warehouse PDA App

Flutter Android app for scan-based field workflows.

The native Android project files are intentionally not generated yet because this workspace does not currently expose the Flutter CLI. Once Flutter is installed, run:

```bash
flutter create --platforms=android .
```

Then keep the existing `pubspec.yaml` and `lib/main.dart` shape as the starting point.

